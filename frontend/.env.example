# URL base del API Nest iFactu (coincide con PORT en backend/.env; por defecto 3002).
# Nexa suele usar :3000 — no mezclar con el API iFactu.
# Reinicia `npm run dev` tras cambiar este valor.
VITE_API_URL=http://127.0.0.1:3002/api
